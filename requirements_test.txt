# Test dependencies for the Blauberg S21 Extended integration.
#
# The suite deliberately avoids depending on Home Assistant itself: see
# tests/hass_stub.py for the reasoning. That means the third-party packages the
# integration imports directly have to be listed here explicitly.
pytest>=7.4
pytest-asyncio>=0.23
pymodbus>=3.6.0,<4.0
voluptuous>=0.13      # imported by config_flow.py
PyYAML>=6.0           # reading services.yaml in test_metadata.py
psutil>=5.9           # socket inspection in the leak assertions
